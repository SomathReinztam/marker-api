version: "3.9"

services:
  marker-api:
    image: savatar101/marker-api:0.3
    container_name: marker-api
    ports:
      - "8051:8000"
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
    restart: unless-stopped